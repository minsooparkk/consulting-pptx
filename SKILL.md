---
name: "consulting-pptx"
description: "Pretendard·네이비/블루 양식의 편집 가능한 컨설팅 보고용 PPTX 제작·재구성·스타일 수정. 원고를 슬라이드별 주제와 결론형 Header, 근거 표·도식으로 바꾸거나 기존 PPT의 글꼴·테마를 통일할 때 사용한다. Create or restyle editable Korean consulting presentations with fixed title, summary Header and Footer."
---

# 컨설팅 보고용 PPTX

## 실행 원칙

- 내용의 논리와 근거를 먼저 설계하고 시각화한다. 한 슬라이드에는 하나의 중심 메시지만 둔다.
- 기본 양식은 Pretendard, 흰 배경, 네이비 상단 제목 밴드, 블루 강조색, 연회색 본문이다.
- 제목은 네이비 밴드 안의 짧은 주제명이다. Header는 그 아래 흰 영역의 완결된 핵심 요약문이다. 둘을 합치거나 표의 열 제목과 혼동하지 않는다.
- 제목·Header·구분선·Footer·상단 밴드는 공통 마스터/레이아웃으로 관리한다. 본문은 실제 정보 관계에 따라 자유롭게 설계한다.
- 제목과 Header의 문구, 본문 표·도식은 PowerPoint에서 직접 편집 가능해야 한다.
- 사용자가 명시한 현재 지시를 우선한다. 기본 장수는 없다. 특정 과정·인물·사례를 자동으로 추가하지 않는다.

## 리소스 선택

파일 경로는 이 SKILL.md가 있는 폴더를 기준으로 해석한다. 특정 OS·사용자 홈·앱 경로를 가정하지 않는다.

1. 모든 작업: `references/design-spec.md`, `references/content-and-qa.md`를 읽는다.
2. 새 자료 제작 또는 기본 양식 적용: `assets/consulting-template.pptx`를 재사용하고 `assets/design-tokens.json`을 기하·서식의 기본값으로 삼는다.
3. PPTX 구현: `references/pptx-implementation.md`를 필요한 범위에서 읽는다.
4. 패키지 구조 검수: `scripts/validate_deck.py`를 사용한다. 이 검사는 렌더링 검사를 대신하지 않는다.
5. 현재 호스트에 PPTX, DOCX, PDF 등 파일 작업 스킬이 있다면 해당 입력·출력 작업에 함께 사용한다. 없는 스킬이나 도구를 있다고 가정하지 않는다.

## 작업 순서

### 1. 자료와 범위 확정

- 첨부 자료를 빠짐없이 확인하고 내용자료, 디자인 참고자료, 수정 대상 결과물로 구분한다.
- 디자인 참고자료의 인물·브랜드·사례·수치는 내용자료에 없으면 가져오지 않는다.
- 스타일만 수정하는 요청이면 기존 파일을 열어 대상 서식만 수정한다. 내용·노트·순서·개수·도식 관계·편집 가능성은 보존한다.
- 디자인 우선순위: 현재 명시 지시 > 승인된 기존 양식 > 참고자료의 실제 시각 양식 > 이 스킬의 기본값.
- 참고 PPTX가 네이티브인지 이미지형인지 먼저 확인한다. 이미지형 PPTX에 남아 있는 미사용 Office 기본 테마를 실제 화면의 글꼴·색상으로 오인하지 않는다.

### 2. 내용과 메시지 설계

각 슬라이드에 대해 `주제 제목 / Header / 본문 근거 / 표현 방식 / 원자료 위치 / 미확정 사항`을 정리한다.

- 제목: 한 줄의 짧은 명사형 주제. 같은 섹션에서 반복할 수 있다.
- Header: 결론·권고·시사점 또는 수행 범위/절차를 요약한 1~2문장. 제목을 풀어 쓰거나 소개 문구로 채우지 않는다.
- 조건부 결론은 핵심 가정을 Header에서도 명시한다. 본문에 없는 주장과 수치를 만들지 않는다.
- Header만 이어 읽어 중복·비약·누락을 고친 뒤 제작한다.
- 긴 구두 설명·진행 메모·상세 출처는 발표자 노트로 옮긴다. 핵심 조건은 본문에도 남긴다.

### 3. 시각화와 양식 적용

- Pretendard 설치·이용 가능 여부를 확인한다. 실제 설치 글꼴의 Regular/Bold를 사용한다. 설치가 없으면 무단 대체나 동일 글꼴이라는 주장을 하지 말고 사용자에게 대체/설치 필요를 알린다.
- 네이비 밴드의 제목은 흰색 Bold, 아래 Header는 짙은 회색 Bold로 고정한다.
- Header는 화면상 2줄을 우선하고 최대 3줄로 제한한다. 길이에 따라 본문을 이동하거나 글자를 자동 축소하지 않는다.
- 표·프로세스·스윔레인·트리·타임라인·검토 매핑·카드 중 실제 관계에 맞는 방식을 선택한다. 카드나 목록 하나로 모든 페이지를 처리하지 않는다.
- 실제 흐름이 있을 때만 화살표를 쓴다. 실측·검증되지 않은 점수·순위·비율·차트 좌표는 만들지 않는다.
- 본문은 기본 15~16pt, 표·도식은 14~16pt로 유지한다. 내용이 많으면 압축하거나 페이지를 분리한다.
- Footer에는 왼쪽 발표 전체 제목과 오른쪽 현재 페이지 번호만 둔다. 출처·로고·날짜·회사명 별도 표기는 금지한다.
- 템플릿의 예시 문구는 모두 교체하고 사용하지 않은 예시 슬라이드를 삭제한다. 예시 자료를 실제 근거로 취급하지 않는다.

### 4. 사실과 산출물의 검증

- 사실, 가상 사례, 조건부 결론, 미검증 제품 정보, 재계산 필요 수치를 구분한다.
- 수치가 충돌하면 임의로 맞추지 않는다. 미확정 수치는 단정형 본문에서 제외하거나 확인 필요로 표시한다.
- 각 슬라이드의 주요 주장과 수치를 원자료의 절·문단·페이지 또는 URL에 대응시켜 노트에 남긴다. 확인사항이 많으면 부록을 만든다.
- 네이티브 텍스트·표·도형·연결선·편집 가능한 차트를 사용한다. 슬라이드 전체나 핵심 도식을 이미지 한 장으로 붙이지 않는다.
- 글꼴 변경 시 보이는 텍스트뿐 아니라 레이아웃, 테마의 major/minor 및 동아시아 글꼴도 함께 변경한다.
- 새 파일로 저장하고 다시 열어 내용·서식·페이지·노트·패키지 무결성을 확인한다. 스타일 수정이면 수정 전후 내용을 직접 비교한다.

### 5. 검수와 전달

- `python scripts/validate_deck.py OUTPUT.pptx`로 기본 네이비/Pretendard 양식을 검사한다. 명시적으로 다른 양식을 사용했다면 `--structure-only`를 사용하고 해당 양식의 서식은 별도로 검수한다.
- 가능한 환경에서는 PowerPoint 또는 신뢰할 수 있는 렌더러로 실제 슬라이드를 렌더링해 전체를 확인하고 수정한다.
- 폰트 폭 계산, 좌표 검사, ZIP/XML 재열기는 시각 검수와 구분한다. 렌더링이 불가능하면 시각 검수를 했다고 주장하지 않는다.
- 결과물은 편집 가능한 PPTX로 제공한다. 실제 검수 범위와 중요한 미확정 사항만 간결히 설명한다.
- 실행하지 않은 Codex/Claude 환경, 파일 미리보기, 제품 기능이 검증됐다고 주장하지 않는다.
